// client/src/components/ai/AiHomeSearch.jsx
// Chat UI is now fully integrated in Dashboard.jsx (two-mode system).
// This file is kept as a safe stub so any stale imports don't break the build.
import React from 'react';
export default function AiHomeSearch() { return null; }

